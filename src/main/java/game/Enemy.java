package game;

public class Enemy extends Player
{

    /**
     * @param name The name of the player
     * @param match The match context
     */
    public Enemy(String name, Match match)
    {
        super(name, match);
    }
}
